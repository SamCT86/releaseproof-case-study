{
  "name": "releaseproof-reference",
  "version": "1.0.0",
  "private": true,
  "description": "Public executable reference for exact-artifact release evidence and explicit inconclusive states.",
  "type": "module",
  "scripts": { "test": "node --test", "verify": "node --test" },
  "engines": { "node": ">=20" }
}
